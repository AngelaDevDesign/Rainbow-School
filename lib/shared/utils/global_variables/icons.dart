import 'package:flutter/material.dart';

const Icon BACK = Icon(Icons.arrow_back);
const Icon ERROR = Icon(Icons.error, color: Colors.white);
const Icon TEACHER = Icon(Icons.account_circle);

const double ICONSIZE = 0.1;